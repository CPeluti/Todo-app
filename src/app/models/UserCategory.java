package models;

public class UserCategory extends Category{

    public UserCategory(String type, String icon, String description, User user) {
        super(type, icon, description, user);
    }
}