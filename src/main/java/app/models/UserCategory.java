package app.models;

import app.models.Category;
import app.models.User;

public class UserCategory extends Category {

    public UserCategory(String type, String icon, String description) {
        super(type, icon, description);
    }

    public String getIcon(){
        return this.getIcon();
    }
}